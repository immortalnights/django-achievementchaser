{
    debug
}

127.0.0.1, vite-frontend-copy-production.up.railway.app, steam.seventh.space {
    encode gzip

    handle /graphql/* {
        reverse_proxy {$BACKEND_HOSTNAME}:{$BACKEND_PORT}
    }

    handle {
        root * ./dist
        try_files {path} ./dist/index.html
        file_server
    }
}
