{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python src/manage.py migrate && python src/manage.py collectstatic --noinput && cd src && gunicorn -b 0.0.0.0:$PYPORT achievementchaser.wsgi",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 2
  }
}
